# Archive Report

**Change**: aporte-cobro-unificado
**Archived**: 2026-08-05
**Verdict**: PASS
**Artifact store mode**: both (OpenSpec files + Engram observation `sdd/aporte-cobro-unificado/archive-report`)
**Archived to**: `openspec/changes/archive/2026-08-05-aporte-cobro-unificado/`

---

## Final-State Facts

- **Delivery**: shipped as **4 chained PRs, all merged to main** (stacked-to-main):
  - PR #11 — catch-up (absorbed dev→main incl. slice A commits)
  - PR #12 — slice B (API auto-generation; merge `f98e747`)
  - PR #13 — slice C (store + unified UI form; merge `df37390`)
  - PR #14 — slice D (cross-path dedup matrix + final gate; merge `e6834e2`)
  - Branch `feat/aporte-cobro-unificado-slice-d` == `origin/main` content.
- **Verification (verify-report, all real execution)**:
  - `pnpm --filter @otb/api test` — **82/82 green** (5 suites: aporte-definicion 25, socios-aportes 22, aportes-generacion 12, aportes-bulk-all-pagos 15, aporte-dedup 8)
  - `pnpm typecheck --skip-nx-cache` — **13/13**
  - `pnpm --filter @otb/web build` — compiles
  - Real `packages/db/otb.db` smoke (non-destructive): 219 records intact, **0** with `aporte_id IS NOT NULL`, partial unique index `aporte_dedup_unico` present (migration `0006`), **0** duplicate `(socio,aporte,mes,gestion)` groups
  - **49/49 delta scenarios compliant** (aporte-definitions 23, payments 9, socios-estados 17), both INVERTED archived behaviors re-proven with evidence (generation at assignment/membership; assertion flip confirmed in PR #12 diff).
- **No post-verify remediation needed**; nothing changed after verify-report (no warnings fixed in later commits).

## Spec Sync Summary

| Domain | Action | Details |
|--------|--------|---------|
| `aporte-definitions` | Updated (1 MODIFIED requirement + 1 MODIFIED/renamed requirement) | **"Definition CRUD — Aportes"** reworked: server-generated UUID `id` (client `id` ignored), `socioIds` assignment with generation in one transaction, response `201 { definiciones, generados }`, "a nadie" → 0 records. Removed the superseded "Create a full definition" client-slug scenario; added 6 scenarios (a nadie, ignored client id, socioIds generation, union dedup, non-permitted assignment kept, unknown socioId 400); kept all validation + PUT/DELETE scenarios. **"Group Application — aplicaGrupoId"** RENAMED from "(Dynamic, NOT Materialized)" to "(Dynamic, Generation at Assignment/Membership)" and INVERTED: current members generate at assignment, future members at membership, records never deleted on member removal, join still never materialized. Description/Frontend Spec/Validation/Error tables updated to the unified flow; DELETE Guard and Seed requirements preserved unchanged. |
| `payments` | Updated (2 ADDED requirements) | **"Auto-Generation at Assignment (New Trigger)"** (4 scenarios: definition creation with assignment, socio save, membership-time generation, non-permitted exclusion without 409) and **"Mandatory Dedup Invariant"** (5 scenarios: creation-with-assignment re-run, socio re-save idempotent, manual bulk re-run `{count:0,items:[]}`, single endpoint re-run, membership re-assertion). All existing atomic-payment / enforcement / filtering requirements preserved unchanged. |
| `socios-estados` | Updated (2 requirements ADDED — flagged MODIFIED in delta but absent from main) | **"Socio Model — aporteIds Multiselect (Many-to-Many)"** (10 scenarios) and **"Group Inheritance — Read-Only Inherited Aportes (Dynamic)"** (7 scenarios) did NOT exist in the main spec — a leftover gap from the archived `tipos-aporte-cobros` change whose archive never merged them. Inserted with the updated (INVERTED) content: socio POST/PUT generate records in the same transaction, dedup idempotency, removals never delete records, membership SHALL generate records (replacing "NO record by membership alone"), non-permitted keeps assignment with 0 records, read-only `aportesInherited` chips unchanged. |

**Merge integrity**: requirements matched/inserted by name; all requirements not mentioned in the deltas preserved verbatim (e.g. `aporte-definitions` DELETE Guard + Seed, payments atomic-payment blocks, socios-estados catalog/migración/baja/enforcement blocks); scenario counts after merge equal preserved + delta (27 / 27 / 43); no `(Previously: ...)` diff-annotations or superseded clauses leaked into main specs.

## Inverted Archived Behaviors (merged into main)

1. **"Group Application MUST NOT pre-generate aporte records"** → now GENERATES at assignment (current members) and at membership time (future members).
2. **"NO aporte record SHALL be generated by membership alone"** → records ARE generated at membership time.

Both were re-verified with passing-test evidence in the verify-report (49/49 compliant; assertion flip confirmed in the PR #12 diff).

## Known Follow-Up (not a blocker)

- **Read-time inheritance chips do not filter `activo`** (pre-existing inconsistency, NOT introduced by this change): `aportesInherited` (`routes/socios.ts`) resolves chips without filtering `activo`, while generation (`definicionesDeGrupos`, design D20) only applies ACTIVE definitions — a member may see a chip for an inactive definition with no new records. Documented in design.md Open Questions + verify-report WARNING; kept out of scope. **Recommend**: decide in a future change whether chips should also filter `activo`.
- Deferred (per design, user decision): removal of `POST /api/aportes`, `/bulk`, `/bulk/all` — retained as dedup-safe endpoints (D17); shared `Toast` component in `@otb/ui` as a possible follow-up to the local inline toast (D22).

## Archive Contents

- proposal.md ✅
- specs/ ✅ (aporte-definitions, payments, socios-estados)
- design.md ✅
- tasks.md ✅ (13/13 tasks complete)
- verify-report.md ✅
- archive-report.md ✅

## Source of Truth Updated

The following main specs now reflect the new behavior:
- `openspec/specs/aporte-definitions/spec.md`
- `openspec/specs/payments/spec.md`
- `openspec/specs/socios-estados/spec.md`

## SDD Cycle Complete

The change has been fully planned, implemented, verified, and archived. Ready for the next change.
